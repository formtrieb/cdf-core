# Profile Test Fixtures

Minimal valid profiles used by `extends-resolver.test.ts`,
`profile-discovery.test.ts`, `profile-coverage.test.ts`, and
`profile-diff.test.ts`. Kept intentionally small — only fields needed
to exercise the algorithm under test.
